# 实现计划：账号密码登录

## 概述

本计划将账号密码登录功能分解为增量式的编码任务，每个任务都建立在前一个任务的基础上，确保代码始终处于可运行状态。

## 任务

- [x] 1. 添加请求 Schema
  - 在 `app/schemas/user.py` 中添加 `AccountLoginRequest` 类
  - 包含 account、password、role 字段
  - _需求: 1.1, 3.6_

- [x] 2. 实现账号密码登录端点
  - [x] 2.1 在 `app/api/auth.py` 中添加 `/auth/login/account` 端点
    - 接收 AccountLoginRequest 请求
    - 查询或创建用户（以 account 作为 open_id）
    - 生成 JWT Token
    - 复用 `_get_user_info()` 构建响应
    - _需求: 1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 2.3, 2.4_

  - [x]* 2.2 编写属性测试：密码无关性
    - **属性 1: 密码无关性**
    - **验证: 需求 1.1**

  - [x]* 2.3 编写属性测试：账号唯一性
    - **属性 2: 账号唯一性**
    - **验证: 需求 1.3, 1.4**

- [x] 3. 检查点 - 确保所有测试通过
  - 确保所有测试通过，如有问题请询问用户

- [x] 4. 验证响应格式和默认值
  - [x]* 4.1 编写属性测试：响应结构完整性
    - **属性 3: 响应结构完整性**
    - **验证: 需求 2.1, 2.2, 2.3, 2.4, 4.1**

  - [x]* 4.2 编写属性测试：默认值正确性
    - **属性 4: 默认值正确性**
    - **验证: 需求 3.1, 3.2, 3.4, 3.5**

  - [x]* 4.3 编写属性测试：角色参数正确性
    - **属性 5: 角色参数正确性**
    - **验证: 需求 3.6**

- [x] 5. 最终检查点 - 确保所有测试通过
  - 确保所有测试通过，如有问题请询问用户

## 备注

- 标记 `*` 的任务为可选任务，可跳过以加快 MVP 开发
- 每个任务都引用了具体的需求以便追溯
- 检查点确保增量验证
- 属性测试验证通用正确性属性
